name: GraphSAGE
description: GraphSAGE for fraud detection

# Model architecture
model:
  hidden_dim: 64
  num_layers: 2
  dropout: 0.5
  aggr: mean

# Training hyperparameters
training:
  epochs: 200
  lr: 0.01
  weight_decay: 0.0005
  
